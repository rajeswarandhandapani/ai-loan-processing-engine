# ============================================================================
# AI Loan Processing Engine — Backend Environment Configuration
# ============================================================================
# Copy this file to `.env` and fill in your real credentials:
#     cp .env.example .env
#
# NOTE: `.env` is git-ignored and must NEVER be committed. This `.env.example`
# contains placeholders only. All values below map to fields in app/config.py.
# Unset optional values fall back to the defaults defined there.

# ----------------------------------------------------------------------------
# Azure Document Intelligence  (document OCR / form + table extraction)
# ----------------------------------------------------------------------------
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/
AZURE_DOCUMENT_INTELLIGENCE_KEY=<your-document-intelligence-key>

# ----------------------------------------------------------------------------
# LLM Provider Selection
# ----------------------------------------------------------------------------
# Supported values: "azure" (Azure OpenAI) or "anthropic" (Claude)
LLM_PROVIDER=azure

# ----------------------------------------------------------------------------
# Azure OpenAI  (chat completions + embeddings for vector search)
# ----------------------------------------------------------------------------
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/
AZURE_OPENAI_API_KEY=<your-azure-openai-key>
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME=text-embedding-ada-002
AZURE_OPENAI_API_VERSION=2024-06-01

# ----------------------------------------------------------------------------
# Azure AI Search  (vector search over lending policy docs — RAG)
# ----------------------------------------------------------------------------
AZURE_SEARCH_ENDPOINT=https://<your-resource>.search.windows.net
AZURE_SEARCH_KEY=<your-search-admin-key>

# ----------------------------------------------------------------------------
# RAG / Vector Store  (policy retrieval — see app/rag/README.md)
# ----------------------------------------------------------------------------
# Defaults are fully local: policy search works with no Azure account and no
# extra installs. Just index once:
#     uv run python scripts/index_lending_policy.py
#
# Where the policy vectors live: "chroma" (local, default) or "azure" (AI Search).
VECTOR_STORE_PROVIDER=chroma
# How text becomes vectors:
#   local        — all-MiniLM-L6-v2 on ONNX, bundled with ChromaDB (default)
#   huggingface  — sentence-transformers; any Hub model, needs `uv sync --group local-rag`
#   azure        — Azure OpenAI embeddings (hosted, costs money)
EMBEDDING_PROVIDER=local
# ChromaDB data directory. Relative paths resolve against backend/.
CHROMA_PERSIST_DIR=.chroma
# Only used when EMBEDDING_PROVIDER=huggingface.
HUGGINGFACE_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# Policy PDF parser: "auto" (Document Intelligence if configured, else pypdf),
# "pypdf" (local, text-layer PDFs only), or "azure_di" (OCR via Azure).
POLICY_PDF_PARSER=auto
#
# To use hosted Azure AI Search instead, set BOTH (the index schema is fixed at
# 1536 dimensions, so it only accepts Azure embeddings), then re-index with --reset:
#     VECTOR_STORE_PROVIDER=azure
#     EMBEDDING_PROVIDER=azure

# ----------------------------------------------------------------------------
# Azure AI Language  (sentiment analysis + entity extraction)
# ----------------------------------------------------------------------------
AZURE_LANGUAGE_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/
AZURE_LANGUAGE_KEY=<your-language-key>

# ----------------------------------------------------------------------------
# Anthropic Claude  (alternative LLM provider — used when LLM_PROVIDER=anthropic)
# ----------------------------------------------------------------------------
ANTHROPIC_API_KEY=<your-anthropic-api-key>
ANTHROPIC_MODEL=claude-sonnet-5

# ----------------------------------------------------------------------------
# LangSmith Tracing  (optional — debugging / monitoring of LangChain runs)
# ----------------------------------------------------------------------------
LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_API_KEY=<your-langsmith-api-key>
LANGSMITH_PROJECT=ai-loan-processing-engine

# ----------------------------------------------------------------------------
# Application Settings
# ----------------------------------------------------------------------------
APP_NAME=AI Loan Processing Engine
DEBUG=false

# ----------------------------------------------------------------------------
# Document Intelligence Cache  (cache extraction results to cut API cost)
# ----------------------------------------------------------------------------
DOCUMENT_CACHE_ENABLED=true
DOCUMENT_CACHE_DIR=.cache/document_intelligence
